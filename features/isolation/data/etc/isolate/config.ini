[global]
	verbose = yes

[isolate "system"]
	rootdir = /root
	stdin = /dev/console
	stdout = /dev/console
	init = $INIT $initargs
	unshare = uts,ipc,sysvsem,pid,mounts
	cap-drop = cap_sys_boot
	#restart = 1
